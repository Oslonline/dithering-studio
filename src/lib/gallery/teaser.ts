/** Tiles shown on the homepage gallery section. */
export const HOME_GALLERY_TEASER_COUNT = 6;

/** Top popular items fetched before random teaser selection (pool for shuffle). */
export const HOME_GALLERY_TEASER_POOL_SIZE = 32;
